version: '3.8'

services:
  browser-optimizer-mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: browser_optimizer_mcp
    volumes:
      - ../browser_optimizer:/app/browser_optimizer
      - ../.env:/app/.env
    restart: unless-stopped
    stdin_open: true
    tty: true
